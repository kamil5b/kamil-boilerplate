development:
  dialect: postgres
  datasource: postgres://postgres:your_password_here@localhost:5432/kamil_boilerplate?sslmode=disable
  dir: migrations
  table: migrations

production:
  dialect: postgres
  datasource: ${DATABASE_URL}
  dir: migrations
  table: migrations
